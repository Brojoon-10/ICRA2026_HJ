<?xml version="1.0"?>
<package format="2">
  <name>bridge_sector_tuner_3d</name>
  <version>0.0.0</version>
  <description>
    HJ: Bridge centerline / heading-lock sector tuner.
    Operates on the smoothed centerline CSV (pre-optimization), not on the
    raceline waypoints. Produces per-map bridge_sectors.yaml that the
    racing-line optimizer consumes to either pin n=0 (force_center) or bound
    abs(chi) below chi_max inside bridge zones (with optional pre/post
    extension).
  </description>

  <maintainer email="hj@todo.todo">hj</maintainer>
  <license>TODO</license>

  <buildtool_depend>catkin</buildtool_depend>
  <build_depend>rospy</build_depend>
  <build_depend>std_msgs</build_depend>
  <build_export_depend>rospy</build_export_depend>
  <build_export_depend>std_msgs</build_export_depend>
  <exec_depend>rospy</exec_depend>
  <exec_depend>std_msgs</exec_depend>

  <export>
  </export>
</package>
